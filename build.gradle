subprojects {
    apply plugin: "java"
    apply plugin: "eclipse"

    repositories {
        mavenCentral()
    }

    eclipse.project {
  	    natures 'org.eclipse.buildship.core.gradleprojectnature'
    }
}